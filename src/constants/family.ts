export interface Family {
  id: string;
  name: string;
  inviteCode: string;
  createdAt: string;
  updatedAt: string;
}

export interface FamilyDTO {
  id: string;
  name: string;
  invite_code: string;
}
